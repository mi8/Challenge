import { TypecolorsPipe } from './typecolors.pipe';

describe('TypecolorsPipe', () => {
  it('create an instance', () => {
    const pipe = new TypecolorsPipe();
    expect(pipe).toBeTruthy();
  });
});
