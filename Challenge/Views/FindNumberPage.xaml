<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Challenge.Views.FindNumberPage">
    <ContentPage.Content>
        <StackLayout VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand">
            <Label Text="Run to find a number under 20 try !" FontSize="Title"/>
            <Button x:Name="LaunchApp" Text="Run" Command="{Binding RunFindNumber}"/>
            <Label Text="{Binding GoodNumber, StringFormat='Number generated is {0}'}"/>
            <Label Text="{Binding Number.Try, StringFormat='Application find it in {0} tries'}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>